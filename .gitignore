# Dependencies
node_modules/
# This project uses Bun exclusively (since v2.24.0) — npm/pnpm/yarn lockfiles
# are stale artifacts. bun.lock is the only committed lockfile.
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build artifacts
dist/
build/
*.tsbuildinfo

# Next.js (web/ workspace)
web/.next/
web/out/
web/next-env.d.ts.bak

# Environment & secrets
.env
.env.*
!.env.example
.npmrc

# Credentials & keys (never commit)
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
credentials/

# Runtime data (SQLite, sessions, logs)
data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.log
logs/
.telegram-session
qr-login.html
qr-login.png

# Docker / infra with prod-specific values (belongs to mcp-telegram-infra)
docker-compose.prod.yml
docker-compose.override.yml

# IDE / editor
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# Personal planning artifacts (not for public repo)
claudedocs/
.claude/

# Test coverage
coverage/
.nyc_output/

# Ralphex (autonomous execution loop) — local only
.ralphex/
.review-context/

# Bun migration spike artifacts (local-only, decision doc lives in claudedocs/)
spike/
