# Dependencies
node_modules/
# This project uses pnpm exclusively — package-lock.json is a stale npm artifact.
# Corepack pin via package.json#packageManager keeps contributors on pnpm.
package-lock.json
yarn.lock

# Build artifacts
dist/
build/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example
.npmrc

# Credentials & keys (never commit)
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
credentials/

# Runtime data (SQLite, sessions, logs)
data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.log
logs/
.telegram-session
qr-login.html
qr-login.png

# Docker / infra with prod-specific values (belongs to mcp-telegram-infra)
docker-compose.prod.yml
docker-compose.override.yml

# IDE / editor
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# Personal planning artifacts (not for public repo)
claudedocs/
.claude/

# Test coverage
coverage/
.nyc_output/

# Ralphex (autonomous execution loop) — local only
.ralphex/
.review-context/

# Bun migration spike artifacts (local-only, decision doc lives in claudedocs/)
spike/
